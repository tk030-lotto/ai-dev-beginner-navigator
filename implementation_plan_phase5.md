# 実装計画書: Phase 5 総合品質・セキュリティ・実動検証

本計画は、「AI開発初心者お助けナビ」の全30プラグイン実装完了（Phase 4）を受け、仕様書第10章（セキュリティ・プライバシー）、第18章（実装上の禁止事項）、および第19章（完成条件チェックリスト）に完全準拠しているかを検証・監査するための計画です。

---

## 目的とスコープ
1. **セキュリティ・プライバシー検証**: 秘匿情報サニタイザー実動、警告表示、完全クライアントサイド完結の確認。
2. **破壊的操作の抑止確認**: 危険コマンド判定における自動実行抑止、安全な代替手順提示の確認。
3. **総合UI/UX・機能実動検証**: ブラウザ上での検索、5段構成解決ビュー、プロンプト生成・コピー、履歴モーダル（入出力含む）、レスポンシブ表示の動作確認。
4. **仕様書第19章（完成条件チェックリスト 全15項目）監査**: 受入基準15項目の網羅的充足チェック。
5. **記録更新とPhase 6公開準備**: RECORD.md の更新およびGitコミット。

---

## 1. セキュリティ & プライバシー実動検証 (仕様書 第10章・第18章)
- **秘匿情報サニタイザー検証 (`src/core/sanitizer.ts`, `src/ui/components/prompt-box.ts`)**:
  - APIキー（`sk-...`、`ghp_...`、`AKIA...`、`-----BEGIN PRIVATE KEY-----` 等）入力時の `[REDACTED]` 置換動作確認。
  - サニタイズ検知時のUI警告表示確認。
  - プロンプト生成ボックス下部の注意文言（「秘密情報が含まれていないことを確認してからAIへ送信してください。」）常時表示確認。
- **完全クライアントサイド動作の確認**:
  - 外部サーバーへの通信が一切発生しないことの確認。

---

## 2. 破壊的操作の抑止確認 (仕様書 第18章)
- **安全な情報提示の徹底**:
  - `cmd-risk-analyzer`、`git-risk-checker` 等において、`rm -rf`, `git reset --hard`, `git push -f` などの危険コマンドが自動実行される仕組みが存在しないことの確認。
  - 危険度判定（高・中・低）の理由、影響範囲、および「安全な代替手順」のみを画面に提示する構造であることを確認。

---

## 3. UI/UX・機能実動検証 (ブラウザE2E)
- **開発サーバー起動**:
  - `npm run dev` によるローカルサーバー起動。
- **実動シナリオ検証**:
  1. **ヒーロー検索・プレースホルダー**:
     - プレースホルダーの自動ローテーション動作確認。
     - 初心者キーワード（例: 「赤い文字」「消えそうで怖い」）入力時の即時インクリメンタル検索動作とスコア順ソート確認。
  2. **カテゴリフィルタ**:
     - 全件（30件）および各カテゴリ（Error: 5, AI: 10, Git: 6, File: 5, Dev: 4）のタブ切り替えと件数表示の一致確認。
  3. **解決ビュー（5段構成）**:
     - カードクリックによる詳細遷移（URLハッシュ `#/plugin/:id`）。
     - ①前提知識 ②手順 ③注意 ④AI相談 ⑤関連 の5セクションが仕様書第13.3章通り正しく表示されることの確認。
  4. **プロンプト生成 & クリップボード連携**:
     - OS・言語の切り替えによるプロンプト動的置換。
     - 「プロンプトをコピー」クリック時のクリップボード格納とToast通知。
     - AI Web直接リンク（ChatGPT, Claude, Gemini）のURLパラメータ構築確認。
  5. **履歴管理モーダル**:
     - 閲覧履歴のLocalStorage自動蓄積確認。
     - 履歴モーダルからの閲覧、個別削除、全消去。
     - JSONエクスポート（ダウンロード）およびJSONインポートの実動確認。
  6. **レスポンシブ & スタイリング**:
     - デスクトップ幅（1280px）およびモバイル幅（375px）でのレイアウト崩れの有無確認。
     - プロトコル第18条「プロジェクト統計ツール」準拠シックなミニマル・ダークUIの整合性確認。

---

## 4. 仕様書第19章（完成条件チェックリスト 15項目）の監査
以下の全15項目に対する合致状況を精査し、検証記録を作成：
1. Coreが起動する
2. PluginをRegistryへ登録できる
3. 30Pluginを登録できる
4. 初心者表現から検索できる
5. 検索結果を表示できる
6. 解決手順を表示できる
7. 注意事項を表示できる
8. AI質問文を生成できる
9. 質問文をコピーできる
10. 履歴を保存できる
11. 履歴を削除できる
12. 履歴をJSON出力できる
13. 秘匿情報の基本的なマスク処理が動作する
14. 破壊的コマンドを自動実行しない
15. READMEと仕様書の内容と実装が一致する

---

## 5. 記録の更新 & コミット
- 検証結果レポートの作成（`walkthrough_phase5.md`）。
- `RECORD.md` の Phase 5 完了ステータス更新。
- Gitマイクロコミットおよびプッシュ。

---

## 検証手順（Verification Plan）

### 自動テスト
- `npm test`: Core基盤・全30プラグイン・検索エンジンの単体/結合テスト（全件合格確認）
- `npm run build`: TypeScript Strict Mode型チェックおよび本番ビルドの正常完了確認

### 実動・ブラウザ検証
- ブラウザサブエージェントまたはブラウザ操作による画面遷移・機能動作・サニタイザー・履歴入出力・レスポンシブの目視確認

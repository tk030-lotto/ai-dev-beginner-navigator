# 実装計画書: Phase 3 代表Plugin実装 & 疎通検証

## 目的とスコープ
本計画は、「AI開発初心者お助けナビ」の各カテゴリを代表する5つのプラグインを実装し、Core基盤（Registry、検索エンジン、サニタイザー、履歴ストア）およびPhase 2で完成したUI（検索、カテゴリタブ、5段構成解決画面、AIプロンプト生成、履歴モーダル）との疎通を検証するものです。

---

## 1. 代表5プラグインの作成 (`src/plugins/`)

各プラグインは `ProblemPlugin` 型に完全準拠し、1ファイル300行以内の単一責務で実装します。

1. **Error代表: `src/plugins/error/err-explainer.ts`**
   - **ID**: `err-explainer`
   - **名称**: エラー翻訳・要約ナビ
   - **カテゴリ**: `error`
   - **概要**: 英語のエラーログやスタックトレースから、何が起きているのか・どこを見ればよいのかを整理して解説します。
   - **初心者フレーズ**: 「赤い文字が出た」「エラーって書いてある」「動かなくなった」「英語がいっぱい」「Traceback」「コンソールエラー」

2. **AI代表: `src/plugins/ai/ai-prompt-creator.ts`**
   - **ID**: `ai-prompt-creator`
   - **名称**: 開発用プロンプト作成ナビ
   - **カテゴリ**: `ai`
   - **概要**: 初心者がAIにコード作成や修正を依頼する際、前提条件・期待する動作・制約事項を明確に伝えるためのプロンプトを組み立てます。
   - **初心者フレーズ**: 「AIにどう頼めばいい？」「プロンプトの書き方」「指示が伝わらない」「思ったのと違うコードが出る」「指示文の作り方」

3. **Git代表: `src/plugins/git/cmd-risk-analyzer.ts`**
   - **ID**: `cmd-risk-analyzer`
   - **名称**: 危険コマンド判定
   - **カテゴリ**: `git`
   - **概要**: ターミナルで実行しようとしているコマンドに、データ消去や上書き、不可逆な変更の危険がないかを判定・解説します。
   - **初心者フレーズ**: 「実行したら危ないコマンド？」「黒い画面で警告が出た」「消えそうで怖い」「resetして大丈夫？」「push -fって何？」「コマンドの意味がわからない」

4. **File代表: `src/plugins/file/file-env-safety.ts`**
   - **ID**: `file-env-safety`
   - **名称**: 環境変数・.env保護ガイド
   - **カテゴリ**: `file`
   - **概要**: APIキーやパスワードなどの機密情報をGitHubに誤って流出させないための、`.env`ファイルと`.gitignore`の安全管理手法を案内します。
   - **初心者フレーズ**: 「.envって何？」「APIキーの隠し方」「GitHubに上げちゃダメ？」「パスワードがバレる」「シークレット」「クレデンシャル」

5. **Dev Support代表: `src/plugins/dev/dev-json-validator.ts`**
   - **ID**: `dev-json-validator`
   - **名称**: JSON構文チェッカー
   - **カテゴリ**: `dev-support`
   - **概要**: `package.json` や設定ファイルで発生しやすいJSONの構文エラー（末尾カンマ、クォーテーション違い等）の原因と修正方法を解説します。
   - **初心者フレーズ**: 「JSONエラーが出た」「カンマの位置がわからない」「Unexpected token」「赤い波線」「JSONが壊れた」「設定ファイルが読めない」

---

## 2. Registry連携エントリ (`src/plugins/index.ts`)
- 5つの代表プラグインをインポート・エクスポート。
- `registerInitialPlugins(registry: PluginRegistry)` 関数を提供し、Registryへの登録を一括実行。

## 3. アプリケーション統合 (`src/main.ts`)
- `src/main.ts` で `registerInitialPlugins(registry)` を呼び出し、アプリ起動時に5つのプラグインを初期ロード。
- 初回ロード時にカテゴリタブの件数バッジ（All: 5, 各カテゴリ: 1）が正しく反映されることを確認。

---

## 4. 自動テスト (`tests/plugins.test.ts`, `tests/index.ts`)
- 代表5プラグインのメタデータ、知識構造、プロンプトテンプレートの妥当性テスト
- Registry登録およびカテゴリ別フィルタリングの動作確認
- 初心者シノニムによる重み付き検索ヒットの確認

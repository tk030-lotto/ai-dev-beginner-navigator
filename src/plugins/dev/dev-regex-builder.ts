import { ProblemPlugin } from '../../types';

export const devRegexBuilderPlugin: ProblemPlugin = {
  metadata: {
    id: 'dev-regex-builder',
    name: '正規表現（RegEx）作成＆検証ナビ',
    category: 'dev-support',
    description: 'メールアドレスや郵便番号、パスワードなどの入力バリデーション用正規表現の組み立てと検証手順です。',
    keywords: ['正規表現', 'RegEx', 'バリデーション', 'パターンマッチ', 'メタ文字'],
    beginnerPhrases: [
      '正規表現が読めない',
      'メールアドレスのチェック',
      '数字だけを抜き出したい',
      '正規表現の作り方がわからない',
    ],
  },
  knowledge: {
    summary:
      '正規表現（Regular Expression）は、文字列のパターンを記号で表現する技術です。`^`（先頭）、`$`（末尾）、`\\d`（数字）、`+`（1回以上繰り返し）などの基本記号を組み合わせることで、複雑な条件分岐を書かずに入力値の検証や抽出ができます。',
    steps: [
      {
        step: 1,
        title: 'マッチさせたい文字列の条件を日本語で書き出す',
        detail:
          '「半角数字のみで3桁ハイフン4桁」「アルファベット大文字小文字と数字を各1文字以上含む8文字以上」のように条件を分解します。',
      },
      {
        step: 2,
        title: '先頭（^）と末尾（$）を必ず付ける',
        detail:
          '完全一致バリデーションを行う場合は `^...$` で囲みます。これを忘れると「途中に条件を満たす部分があればOK」と判定されてしまいます。',
      },
      {
        step: 3,
        title: 'テストケース（OKパターンとNGパターン）で検証する',
        detail:
          '正常な入力例だけでなく、「空文字」「前後にスペース」「日本語が混入」「文字数が足りない」などの境界値で期待通り不合格になるかテストします。',
      },
    ],
    cautions: [
      '複雑すぎる正規表現（入れ子の繰り返しなど）は、ReDoS（正規表現のバックトラックによるサービス停止攻撃）の原因になることがあります。',
      '過度に厳密な正規表現（RFC 5322完全準拠のメールアドレスなど）は保守不能になるため、実務では実用的な範囲に留めるのが推奨されます。',
    ],
  },
  promptTemplates: [
    {
      targetAi: 'ChatGPT',
      title: '正規表現パターン生成プロンプト',
      generate: (context) => {
        const lang = context.language || 'JavaScript / TypeScript';
        const detail = context.detail || '郵便番号または電話番号の入力チェックを行いたいです。';
        return `以下の条件を満たす、${lang}向けの正規表現パターンを作成してください。

【検証したい条件】
${detail}

【お願い】
1. 完全一致用の正規表現パターン文字列
2. 各記号（メタ文字、量指定子、文字クラス）が何をしているのかの丁寧な解説
3. マッチする入力例（OKパターン5選）と、弾くべき入力例（NGパターン5選）のテストデータ
4. ${lang}での具体的なコード例（RegExp.test など）`;
      },
    },
  ],
};

/**
 * AIプラグイン: 実行前セーフティチェック
 * ID: ai-pre-check
 * 「AIに言われたコードやスクリプトを実行してPCが壊れないか」「ファイルが消えないか怖い」という初心者に、
 * 危険な処理（ファイル削除、無限ループ、過度なAPI通信、管理者権限）を事前検知するためのセーフティガイドを提供する。
 */

import { ProblemPlugin } from '../../types';

export const aiPreCheckPlugin: ProblemPlugin = {
  metadata: {
    id: 'ai-pre-check',
    name: '実行前セーフティチェック',
    category: 'ai',
    description: 'AIが生成したコードやスクリプトを実行する前に、ファイル消去・無限ループ・意図しない上書きなどのリスクがないか点検します。',
    keywords: [
      '事前チェック',
      'セーフティ',
      '実行前確認',
      '危険回避',
      'コード点検',
      '暴走防止',
    ],
    beginnerPhrases: [
      'このコード動かして壊れない？',
      '実行前の確認',
      'PC壊れない？',
      '実行して大丈夫？',
      '動かすのが怖い',
      '危険なスクリプトの見分け方',
    ],
  },

  knowledge: {
    summary: '初心者が最も警戒すべきは「ファイルの一括削除（rm -rf / fs.rmSync）」「条件のない無限ループ（while (true)）」「管理者権限（sudo / 管理者として実行）の要求」です。これらを含まないか確認してから実行します。',
    steps: [
      {
        step: 1,
        title: 'ファイル操作命令（削除・上書き）の有無を確認する',
        detail: 'コード内に unlink, rm, remove, drop, delete, truncate などの単語がないか検索し、大切なファイルを消去する記述がないか点検します。',
      },
      {
        step: 2,
        title: 'ループ処理の終了条件を確認する',
        detail: 'while や for ループがある場合、カウント変数が正しく増加・減少しているか、break条件が確実に成立するかを確認します。',
      },
      {
        step: 3,
        title: 'テスト用の別フォルダで試す',
        detail: '不安なファイル操作スクリプトは、本番のプロジェクトではなく、デスクトップに作った空の「test_folder」などでダミーファイルを作って実行テストします。',
      },
    ],
    cautions: [
      'AIが「管理者権限で実行してください」と言ってきた場合、原則として一旦停止し、なぜ管理者権限が必要なのか別のAIやWebで再確認する。',
      'WebスクレイピングやAPI呼び出しループで、ウェイト（sleep）が入っていないコードを実行するとアクセス遮断（BAN）される恐れがあります。',
    ],
  },

  promptTemplates: [
    {
      targetAi: 'All',
      title: 'スクリプト実行前の危険性・副作用レビュー依頼',
      generate: (context) => {
        const os = context.os || 'Windows';
        const lang = context.language || 'Node.js / Python';
        const detail = context.detail || '（実行しようとしているコードやコマンド）';

        return `初学者です。以下のコードを自分のローカルPC（${os}）上で実行しようとしていますが、思わぬ事故や副作用がないか心配です。

【環境】: OS: ${os}, 言語: ${lang}

【実行予定のコード】:
\`\`\`
${detail}
\`\`\`

【確認してほしい項目】
1. このコードを実行すると、PC内のファイルが削除・変更される可能性はありますか？
2. 無限ループでPCがフリーズしたり、外部へ大量のリクエストを送ってしまうリスクはありますか？
3. 初心者が安全に実行するためのテスト手順（ドライランやサンドボックス確認方法）を教えてください。`;
      },
    },
  ],
};

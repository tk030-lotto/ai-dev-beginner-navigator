# AI開発初心者お助けナビ

## 詳細仕様書 — SPECIFICATION.md

* バージョン: 1.0.0
* ステータス: **確定版 / 実装開始可能**
* 設計方式: プラグイン指向アーキテクチャ
* 実行方式: クライアントサイド
* 対象: AIを利用して開発する初心者
* ライセンス: MIT License

---

# 第1章 システム概要

## 1.1 目的

AIを利用した開発において初心者が遭遇する、

* 技術用語がわからない
* 問題をどう表現すればよいかわからない
* エラーの意味がわからない
* AIへ何を質問すればよいかわからない
* AIの回答を理解できない
* AIが提示したコマンドを実行してよいかわからない

という問題を解消する。

本システムは、初心者の自然言語による困りごとを検索入口とし、

```text
初心者の言葉
↓
関連する技術概念
↓
問題の説明
↓
解決手順
↓
注意事項
↓
AIへの質問
```

という一連の流れを提供する。

---

# 第2章 基本設計思想

## 2.1 逆引き検索

ユーザーが専門用語を知らなくても検索できることを最優先とする。

例：

```text
「GitHubに送れない」
「ファイルが消えた」
「赤い文字がいっぱい出た」
「AIにどう頼めばいい？」
```

などの自然な表現を検索対象とする。

---

## 2.2 Micro-Core

Coreは最小限の責務のみを持つ。

Coreの責務：

* アプリケーション起動
* Plugin登録
* Plugin管理
* 検索
* 検索結果表示への遷移
* 履歴管理

Coreに個別問題の知識を持たせない。

---

## 2.3 Plugin分離

問題解決に関する知識はPluginに保持する。

したがって、

```text
Core
  ├── 検索
  ├── 登録
  ├── 履歴
  └── UI制御

Plugin
  ├── 問題
  ├── 初心者表現
  ├── 技術用語
  ├── 解説
  ├── 解決手順
  ├── 注意事項
  └── AI質問テンプレート
```

という責務分離を行う。

---

# 第3章 機能要件

| ID   | 機能     | 仕様                                       |
| ---- | ------ | ---------------------------------------- |
| F-01 | 検索     | 初心者フレーズ、キーワード、名称、説明を検索                   |
| F-02 | カテゴリ   | Error / AI / Git / File / Dev Supportで分類 |
| F-03 | 解決ビュー  | 概要・手順・注意事項を統一表示                          |
| F-04 | AI質問生成 | 状況を差し込んだ質問文を生成                           |
| F-05 | コピー    | 生成した質問文をクリップボードへコピー                      |
| F-06 | AI連携   | Web版AIをユーザー操作で開く                         |
| F-07 | 履歴     | 検索・閲覧・質問生成を記録                            |
| F-08 | 履歴管理   | 履歴の確認・削除・全消去                             |
| F-09 | エクスポート | 履歴をJSONとして保存可能                           |
| F-10 | インポート  | エクスポートした履歴を復元可能                          |

---

# 第4章 非機能要件

## 4.1 対応ブラウザ

以下のモダンブラウザを対象とする。

* Chrome
* Edge
* Firefox
* Safari

原則として現行版を対象とする。

---

## 4.2 ネットワーク

基本機能はクライアントサイドで動作する。

外部通信を必須としない。

AI Webサービスとの連携については、ユーザーが明示的に操作した場合のみ外部サイトへ遷移する。

---

## 4.3 パフォーマンス

目標値：

* 初期表示: 1秒以内
* 検索応答: 50ms以内を目標
* 初期30Plugin: 問題なく検索可能であること

100Plugin以上への拡張を想定するが、性能目標は実装時の測定結果を基準として必要に応じて調整する。

---

# 第5章 Pluginインターフェース

## 5.1 CategoryType

```typescript
export type CategoryType =
  | 'error'
  | 'ai'
  | 'git'
  | 'file'
  | 'dev-support';
```

---

## 5.2 ProblemStep

```typescript
export interface ProblemStep {
  step: number;
  title: string;
  detail: string;
  command?: string;
}
```

---

## 5.3 PromptTemplate

```typescript
export interface PromptTemplate {
  targetAi: 'ChatGPT' | 'Gemini' | 'Claude' | 'All';
  title: string;

  generate: (context: {
    os?: string;
    language?: string;
    userInput?: string;
    detail?: string;
  }) => string;
}
```

---

## 5.4 PluginMetadata

```typescript
export interface PluginMetadata {
  id: string;
  name: string;
  category: CategoryType;
  description: string;
  keywords: string[];
  beginnerPhrases: string[];
}
```

---

## 5.5 ProblemPlugin

```typescript
export interface ProblemPlugin {
  metadata: PluginMetadata;

  knowledge: {
    summary: string;
    steps: ProblemStep[];
    cautions: string[];
  };

  promptTemplates: PromptTemplate[];

  renderCustomTool?: () => HTMLElement | unknown;
}
```

---

# 第6章 検索エンジン

## 6.1 基本方針

検索は完全クライアントサイドで実行する。

検索対象：

* Plugin名称
* description
* keywords
* beginnerPhrases
* summary

---

## 6.2 初期スコアリング

基本スコアは以下を基準とする。

```text
beginnerPhrases : 3
name            : 2
keywords        : 2
description     : 1
summary         : 1
```

概念上のスコア：

```text
Score =
  beginnerPhrases一致 × 3
+ keywords一致 × 2
+ name一致 × 2
+ description一致 × 1
+ summary一致 × 1
```

完全一致、部分一致、複数語一致については実装時に優先順位を定義する。

---

# 第7章 シノニム

初心者の表現を技術概念へ変換するため、シノニム辞書を使用する。

| 初心者表現                        | 内部概念                                 |
| ---------------------------- | ------------------------------------ |
| アップしたい / 載せたい / 公開したい / 送りたい | push / deploy / publish / commit     |
| 消えた / 戻したい / 取り消したい / やり直したい | restore / revert / reset / undo      |
| 動かない / 落ちる / 赤文字 / 止まった      | error / exception / failed / trouble |
| 書き方 / どう指示する / テンプレ          | prompt / instruction / template      |

シノニム辞書はCoreに実装する。

ただし、個別Plugin固有の検索語はPlugin側の`keywords`および`beginnerPhrases`で定義する。

---

# 第8章 初期30Plugin

## Error

1. `err-explainer`
2. `err-cause-finder`
3. `err-log-cleaner`
4. `err-ai-consultant`
5. `err-fix-checker`

## AI

6. `ai-prompt-creator`
7. `ai-instruction-check`
8. `ai-task-delegation`
9. `ai-response-organizer`
10. `ai-todo-extractor`
11. `ai-answer-compare`
12. `ai-diff-analyzer`
13. `ai-input-sanitizer`
14. `ai-pre-check`
15. `ai-post-check`

## Git

16. `cmd-explainer`
17. `cmd-risk-analyzer`
18. `git-concept`
19. `git-risk-checker`
20. `git-terms`
21. `git-trouble-helper`

## File

22. `file-structure`
23. `file-ext-explainer`
24. `file-config-checker`
25. `file-env-safety`
26. `file-cleaner`

## Development Support

27. `dev-md-formatter`
28. `dev-md-table`
29. `dev-json-validator`
30. `dev-ai-vs-human`

---

# 第9章 履歴管理

## 9.1 ActivityLog

```typescript
export interface ActivityLog {
  id: string;
  timestamp: number;

  type:
    | 'search'
    | 'view_plugin'
    | 'generate_prompt';

  payload: {
    query?: string;
    pluginId?: string;
    pluginName?: string;
    generatedPrompt?: string;
  };
}
```

---

## 9.2 保存先

ブラウザ内ストレージを使用する。

基本方針：

* 小規模データ → LocalStorage
* 履歴等の拡張を考慮する場合 → IndexedDB

実装時にはどちらか一方を標準ストレージとして採用し、Coreから直接ストレージ実装へ依存しない構造とする。

---

## 9.3 履歴上限

標準保持件数：

**500件**

上限を超えた場合は古い履歴から削除する。

---

## 9.4 エクスポート

```typescript
exportLogs()
```

JSON形式で履歴をエクスポートする。

---

## 9.5 全消去

```typescript
clearLogs()
```

ユーザー操作によって履歴を全消去できる。

---

# 第10章 セキュリティ・プライバシー

## 10.1 基本原則

ユーザーの入力情報をサーバーへ送信しない。

---

## 10.2 秘匿情報

以下のような情報を検出対象とする。

* APIキー
* AWSキー
* Private Key
* Token
* パスワード等

検出した場合は可能な範囲で、

```text
[REDACTED]
```

へ置換する。

---

## 10.3 重要な制限

本機能は**完全な秘密情報検出を保証するものではない**。

したがってUI上でも、

> 秘密情報が含まれていないことを確認してからAIへ送信してください。

という注意を表示する。

---

# 第11章 AI連携

## 11.1 基本方針

AI APIを直接呼び出さない。

対象：

* ChatGPT
* Gemini
* Claude

生成した質問文を、

```text
生成
 ↓
コピー
 ↓
ユーザーがAI Web版へ貼り付け
```

または対応するWebサービスをユーザー操作で開く。

---

## 11.2 AI別最適化

Pluginごとに、

```typescript
targetAi:
  'ChatGPT'
  'Gemini'
  'Claude'
  'All'
```

を指定できる。

ただし、AIごとの仕様変更に強く依存しないよう、初期版では**AI固有機能ではなく質問文の形式差を中心とした最適化**とする。

---

# 第12章 Plugin登録

Plugin追加時にはRegistryへ登録する。

```typescript
pluginRegistry.register(plugin);
```

Coreは登録されたPluginを検索対象として自動的に扱う。

Plugin固有の検索・表示ロジックをCoreへ追加してはならない。

---

# 第13章 UI

## 13.1 トップ画面

最初に検索欄を表示する。

```text
┌──────────────────────────────────┐
│  何に困っていますか？            │
│                                  │
│  「GitHubにアップできない」       │
│                                  │
└──────────────────────────────────┘
```

検索を主要導線とする。

---

## 13.2 検索結果

各結果には最低限、

* Plugin名称
* 概要
* カテゴリ
* 関連度

を表示する。

---

## 13.3 解決画面

以下の順番で表示する。

```text
① まず知っておくこと

② 解決手順

③ 注意・やってはいけないこと

④ AIに相談する

⑤ 関連する困りごと
```

---

# 第14章 エラー・危険操作への対応

本ツール自体がユーザーのPC上でコマンドを自動実行してはならない。

特に、

```text
git reset --hard
git push -f
rm
del
```

などの破壊的操作については、

**説明・警告・AIへの相談支援まで**とし、自動実行機能は実装しない。

---

# 第15章 オフライン・PWA

初期バージョンではPWA対応を必須としない。

ただし、

* 静的アセット
* Pluginデータ
* 検索エンジン
* 履歴

がローカルで動作する構造にしておき、将来的にService Workerを追加できる構成とする。

---

# 第16章 拡張方針

将来的に以下を追加可能とする。

### 16.1 Plugin追加

```text
30
 ↓
40
 ↓
50
 ↓
100+
```

Coreの基本設計を変更せず追加できることを目標とする。

### 16.2 特化Plugin

* Docker
* Python
* Next.js
* Supabase
* TypeScript
* GitHub Actions

など、技術スタック別のPluginを追加可能とする。

### 16.3 外部Plugin

将来的にGitHub等からPluginを取得する仕組みを検討する。

ただし、**初期バージョンでは外部Pluginの動的読み込みを実装しない。**

---

# 第17章 実装フェーズ

## Phase 1 — Core

* プロジェクト初期化
* TypeScript設定
* Plugin型定義
* Registry
* Search Engine
* Router
* History Store

## Phase 2 — UI

* 検索画面
* 検索結果
* 解決画面
* プロンプト表示
* 履歴画面

## Phase 3 — 初期Plugin

30Pluginを実装する。

ただし、30個を同時に作り込むのではなく、

```text
Core完成
 ↓
代表Plugin数個
 ↓
検索・表示・AI質問生成を検証
 ↓
問題修正
 ↓
残りPlugin追加
```

の順で進める。

## Phase 4 — テスト

* 検索テスト
* Plugin登録テスト
* UIテスト
* 履歴テスト
* サニタイズテスト
* 危険操作警告テスト
* ブラウザ互換性確認

## Phase 5 — 公開準備

* README確認
* LICENSE
* ビルド確認
* PWA対応可否確認
* GitHub公開

---

# 第18章 実装上の禁止事項

以下は禁止する。

1. Coreへ個別Pluginの知識を直接追加すること
2. AI APIを必須化すること
3. ユーザーの入力を無断で外部送信すること
4. コマンドを自動実行すること
5. 破壊的操作を自動実行すること
6. Pluginごとに異なるデータ構造を勝手に作ること
7. 初心者向け説明を専門用語だけで構成すること

---

# 第19章 完成条件

Version 1.0.0は以下を満たした時点で完成とする。

- [ ] Coreがブラウザ上で正常起動する
- [ ] PluginをRegistryへ登録できる
- [ ] 仕様書に定められた30Pluginがすべて登録されている
- [ ] 初心者表現（例：「GitHubに載せたい」）から適切なPluginが検索できる
- [ ] 検索結果一覧が正しくソート・表示される
- [ ] 解決画面で「概要」「手順」「注意事項」が表示される
- [ ] AI質問文が動的に生成され、ワンクリックでクリップボードへコピーできる
- [ ] 履歴がLocalStorageに保存され、一覧確認・削除・JSONエクスポート/インポートができる
- [ ] 秘密情報（APIキー等）の基本的なマスク処理が動作する
- [ ] 破壊的コマンドは解説と警告のみを行い、自動実行しない
- [ ] READMEおよび仕様書の内容と実装が完全に一致している

---

# 第20章 最終設計原則

本プロジェクトの中心は「30個の便利機能」ではない。

中心となるのは、

> **初心者の曖昧な言葉を、AI開発で使える具体的な問題へ翻訳すること**

である。

したがって、今後Pluginを追加する場合も、

```text
困った
 ↓
言葉にする
 ↓
検索する
 ↓
意味を知る
 ↓
解決する
 ↓
必要ならAIに聞く
```

という基本体験を壊さないことを最優先とする。

---

## 仕様ステータス

**SPECIFICATION.md Version 1.0.0**

**確定版**

本仕様をVersion 1.0.0の実装基準とし、実装開始後に発生した変更については、仕様変更として記録する。

仕様に記載されていない機能を、実装者の判断だけで追加してはならない。
